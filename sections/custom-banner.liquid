{% schema %}
{
  "name": "Custom Banner with Popup",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Logo Text",
      "default": "TISSO VISON"
    },
    {
      "type": "text",
      "id": "top_message",
      "label": "Top Message",
      "default": "Find the ideal gift for your loved ones."
    },
    {
      "type": "text",
      "id": "top_button_text",
      "label": "Top Button Text",
      "default": "CHOOSE GIFT"
    },
    {
      "type": "textarea",
      "id": "popup_content",
      "label": "Popup Content",
      "default": "Your popup message or HTML goes here!"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Main Title",
      "default": "The Gift Guide"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Button Text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Button Link"
    },
    {
      "type": "text",
      "id": "footer_text",
      "label": "Footer Text",
      "default": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL"
    }
  ],
  "presets": [
    {
      "name": "Custom Banner with Popup"
    }
  ]
}
{% endschema %}

<section class="custom-banner" style="background-image: url({{ section.settings.background_image | image_url }});">
  <!-- Top bar -->
  <div class="banner-top">
    <div class="logo">{{ section.settings.logo_text }}</div>
    <div class="top-message">{{ section.settings.top_message }}</div>
    <button class="top-btn" id="popup-trigger">
      {{ section.settings.top_button_text }} →
    </button>
  </div>

  <!-- Center content -->
  <div class="banner-content">
    <h1 class="banner-title">{{ section.settings.title }}</h1>
    <p class="banner-subtitle">{{ section.settings.subtitle }}</p>
    <a href="{{ section.settings.cta_link }}" class="cta-btn">
      {{ section.settings.cta_text }} →
    </a>
  </div>

  <!-- Footer -->
  <div class="banner-footer">
    {{ section.settings.footer_text }}
  </div>
</section>

<!-- Popup -->
<div class="popup-overlay" id="popup-overlay">
  <div class="popup-box">
    <button class="popup-close" id="popup-close">×</button>
    <div class="popup-content">
      {{ section.settings.popup_content }}
    </div>
  </div>
</div>

<style>
.custom-banner {
  position: relative;
  width: 100%;
  height: 100vh;
  background-size: cover;
  background-position: center;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  padding: 20px;
  box-sizing: border-box;
}

/* Top bar */
.banner-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20px;
}

.logo {
  font-size: 22px;
  font-weight: bold;
  text-transform: uppercase;
}

.top-message {
  flex: 1;
  text-align: center;
  font-size: 16px;
  font-weight: 500;
}

.top-btn {
  background: yellow;
  padding: 10px 20px;
  font-weight: bold;
  border-radius: 4px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
}
.top-btn:hover {
  background: black;
  color: white;
}

/* Center content */
.banner-content {
  text-align: center;
  margin-top: auto;
  margin-bottom: auto;
}

.banner-title {
  font-size: 52px;
  font-weight: bold;
  text-transform: uppercase;
}

.banner-subtitle {
  max-width: 600px;
  margin: 20px auto;
  font-size: 18px;
  line-height: 1.6;
}

.cta-btn {
  background: black;
  color: white;
  padding: 12px 24px;
  display: inline-block;
  text-decoration: none;
  font-weight: bold;
  border-radius: 4px;
  transition: all 0.3s ease;
}
.cta-btn:hover {
  background: #333;
}

/* Footer */
.banner-footer {
  text-align: center;
  font-size: 14px;
  margin-top: 20px;
  opacity: 0.9;
}

/* Popup */
.popup-overlay {
  display: none;
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0,0,0,0.6);
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

.popup-box {
  background: #fff;
  padding: 30px;
  border-radius: 8px;
  max-width: 500px;
  width: 90%;
  position: relative;
  text-align: center;
  animation: fadeIn 0.3s ease;
}

.popup-close {
  position: absolute;
  top: 10px; right: 10px;
  background: none;
  border: none;
  font-size: 20px;
  cursor: pointer;
}

@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}
</style>

<script>
document.addEventListener("DOMContentLoaded", function() {
  var trigger = document.getElementById("popup-trigger");
  var overlay = document.getElementById("popup-overlay");
  var closeBtn = document.getElementById("popup-close");

  trigger.addEventListener("click", function() {
    overlay.style.display = "flex";
  });

  closeBtn.addEventListener("click", function() {
    overlay.style.display = "none";
  });

  overlay.addEventListener("click", function(e) {
    if(e.target === overlay) {
      overlay.style.display = "none";
    }
  });
});
</script>
